#include <stdio.h>
int main(){
    int size;
    scanf("%d",&size);
    int a[size];

    for(int i=0;i<size;i++){
        scanf("%d",&a[i]);
    }

    for(int i = 1; i <= 6; i++) {
    int count = 0;
    for(int j = 0; j < size; j++) {
        if(a[j] == i) 
        count++;
        }
    printf("%d occurred %d times\n", i, count);
    }
}
