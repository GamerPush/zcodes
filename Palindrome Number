//check whether a given string is a palindrome.
 n=int(input())
 orig=n
 rev=0
 digits=len(str(n))
 for i in range(digits):
     lastdig=n%10
     rev=rev*10+lastdig
     n//=10
 if orig==rev:
         print("Palindrome")
 else:
         print("Not a Palindrome")
