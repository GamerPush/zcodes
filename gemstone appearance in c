#include <stdio.h>
int main(){
    int target,count=0;
    printf("Enter No: ");
    scanf("%d",&target);
    int a[20];

    for(int i=0;i<20;i++){
        scanf("%d",&a[i]);
    }

    for(int i = 0;i<20;i++) {
        if(a[i] == target) 
        count++;
        }
        printf("%d occurred %d times",target,count);
    return 0;
    }
