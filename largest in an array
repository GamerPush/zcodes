#include<stdio.h>
int main(){
int x;
scanf("%d",&x);
int a[x][x];
int large;
for(int i=0;i<x;i++){
    for(int j=0;j<x;j++){
    scanf("%d",&a[i][j]);
        large=a[0][0];
    if(a[i][j]>large){
    large=a[i][j];
    }
}
}
printf("largest number in array is: %d ",large);
return 0;
}
