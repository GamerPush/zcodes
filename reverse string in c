#include<stdio.h>
#include<string.h>
int main(){
char s[100];
scanf("%s",s);
char rev[100];
int l=strlen(s)-1;
int i=0;
while(s[i]!='\0'){
    rev[i]=s[l];
    i++;
    l--;
}
printf("%s",rev);
return 0;
}
